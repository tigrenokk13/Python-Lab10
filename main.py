# Функція для відкриття файлу
def open_file(filename):
    try:
        file = open(filename, "w")
        print("Файл успішно відкрито!")
        return file
    except Exception as e:
        print(f"Не вдалося відкрити файл! Помилка: {e}")
        return None

# Функція для запису рядків у файл
def write_to_file(file):
    file = open_file(filename)
    if file is not None:
        try:

            # (Стешенко Станіслав КН-31.1)
            file.write("Стешенко Станіслав КН-31.1\n")
            file.write("Питання 1: Як вивести текст на екран у Python?\n")

             # (Мороз Володимир КН-31.1)
            file.write("Мороз Володимир КН-31.1\n")
            file.write("Відповідь 1: У Python можна вивести текст на екран за допомогою функції print()."
                       " Якщо вам потрібно вивести кілька рядків тексту, "
                       "можна використовувати кілька викликів print() або спеціальне форматування рядка, "
                       "наприклад, потрійні лапки, щоб оформити текст на кілька рядків.\n")
            file.write("Питання 2:Що таке список у Python, і чим він відрізняється від словника?\n")

            # (Шелест Ігор КН-33.2)
            file.write("Шелест Ігор КН-33.2\n")
            file.write("Відповідь 1:У Python список (list) — це впорядкована змінна колекція елементів, де кожен елемент має свій індекс, починаючи з 0."
                       "Списки дозволяють зберігати елементи різних типів,"
                       "але найчастіше використовуються для послідовностей елементів одного типу. Список можна змінювати: додавати, видаляти або змінювати його елементи. \n")
            file.write("Питання 3:Як записати список рядків у файл, щоб кожен рядок списку займав окремий рядок у файлі?\n")
            
            # Закриття файлу після запису
            file.close()
            print("Дані успішно записані у файл.")
            print("Файл успішно закрито!")
        except Exception as e:
            print(f"Виникла помилка під час запису у файл: {e}")
    else:
        print("Файл не було відкрито, запис неможливий.")

# (Стешенко Станіслав КН-31.1)
# Виклик функцій
filename = "Lab-10.txt"
write_to_file(filename)
